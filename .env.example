# Exemplo de variáveis de ambiente para desenvolvimento
# Copie este arquivo para `.env` e ajuste conforme necessário

# Endereço WebSocket do servidor (ex: ws://host:porta/ws)
VITE_WS_URL=ws://localhost:3001/ws

# Hosts permitidos (separados por vírgula) para Vite `allowedHosts`
# Exemplo: pokecard.wmst.com.br,localhost,192.168.1.8
VITE_ALLOWED_HOSTS=localhost,127.0.0.1

# URL pública do servidor backend
VITE_SERVER_URL=http://localhost:3001